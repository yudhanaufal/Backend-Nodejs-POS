/*async function getLaporanToko(
    startDate,
    endDate
) {
    const response = await fetch(
        `http://localhost:3000/api/laporan/laporantoko/?start_date=${startDate}&end_date=${endDate}`
    );

    return await response.json();
}

module.exports = {
    getLaporanToko
};*/